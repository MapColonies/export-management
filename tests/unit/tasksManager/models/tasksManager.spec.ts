describe('TasksManager', () => {
  describe('#getSum', () => {
    it('should return sum of 2 number', function () {
      // action
      // TBD

      // expectation
      // eslint-disable-next-line @typescript-eslint/no-magic-numbers
      expect(1 + 1).toBe(2);
    });
  });
});
